# Interaction performance

For laggy clicks, typing, dragging, or scrolling, capture an interaction trace.

Separate urgent work from secondary work. Reduce synchronous computation, unnecessary state propagation, DOM work, and long tasks. Consider yielding or workers for heavy CPU work.

Verify INP and the specific interaction trace before/after.
