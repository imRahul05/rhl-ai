# Image performance

Determine whether an image is LCP, above the fold, decorative, or content-critical.

Use an appropriate modern format, responsive dimensions, correct intrinsic size, compression, and loading priority. Lazy-load non-critical images. Avoid lazy-loading the LCP image.

Verify bytes, request timing, decode/render behavior, CLS, and LCP.
