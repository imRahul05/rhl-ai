# Caching

Start by identifying the repeated work and its freshness contract.

Choose among browser/CDN/server/data caches based on ownership and invalidation needs.

Define TTL/revalidation/invalidation, cache key, privacy scope, and failure behavior before implementation.

Verify hit rate, latency, origin load, freshness errors, and memory/storage cost.
